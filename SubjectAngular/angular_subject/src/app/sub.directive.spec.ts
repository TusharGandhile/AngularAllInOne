import { SubDirective } from './sub.directive';

describe('SubDirective', () => {
  it('should create an instance', () => {
    const directive = new SubDirective();
    expect(directive).toBeTruthy();
  });
});
